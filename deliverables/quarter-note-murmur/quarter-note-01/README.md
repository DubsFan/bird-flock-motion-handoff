# Quarter Note — Gentle Orbit

Import the parent `quarter-note-01-murmur.zip` into one Murmur Flock tab. The archive contains exactly one `manifest.json`, sixteen chronological flight PNGs, and three matched sixteen-frame action tracks.

Expected import result:

`Passed: 16 flight poses plus 48 matched landing-action poses. Murmur will switch tracks automatically.`

The fixed path anchor is `(800, 900)` on every 1600 × 1200 frame. All visible pixels use `#0D8EEB`; background pixels are straight alpha with RGB zero. Every frame preserves one rigid note silhouette at constant scale.
